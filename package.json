{
    "scripts": {
        "lint": "eslint ."
    },
    "devDependencies": {
        "eslint": "^7.6.0",
        "eslint-config-google": "^0.5.0"
    },
    "eslintConfig": {
        "extends": "google",
        "rules": {
           "linebreak-style": [
            "error",
            "windows"
            ]
        }
    }
}